Геля-Геля v5 CLEAN

Ключові виправлення:
- вступний екран проходиться рівно ОДИН раз;
- питання мають пріоритет над intro-текстом, тому stale intro більше не запускає цикл Next;
- після кожного кліку перевіряється реальний перехід; повторного blind-click немає;
- чек не видаляється з локальної черги при помилці;
- QR з фото декодується на сервері після швидкого ресайзу, без десятків важких проходів на телефоні;
- чистий root проєкту без вкладених папок.

Для Railway: root directory = /, Dockerfile = Dockerfile, start = node server.mjs.
deploy v5
